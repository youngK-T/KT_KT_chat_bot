# LangChain 관련 (안정적인 0.2.x 버전 사용)
langchain==0.2.14
langchain-openai==0.1.23
langchain-core==0.2.38
langchain-community==0.2.16
langgraph==0.2.3

# OpenAI 패키지 (Azure OpenAI 사용을 위해 필수)
openai>=1.0.0

# Azure 관련 (Mock 테스트에서는 불필요 - 실제 배포시에만 설치)
# azure-storage-blob==12.19.0

# Database (Mock 테스트에서는 불필요 - 실제 배포시에만 설치)
# psycopg==3.1.10

# FastAPI 관련 (Mock 테스트에서는 불필요)
# fastapi==0.104.1
# uvicorn[standard]==0.24.0
pydantic==2.5.0  # TypedDict 대신 사용할 수 있어서 유지

# HTTP 요청
requests==2.31.0

# 수학/과학 계산
numpy==1.24.3

# 문서 처리 (Mock 테스트에서는 불필요)
# pymupdf==1.23.0
# python-docx==1.1.0

# 기타 (Mock 테스트에서는 불필요)
# chromadb==0.4.15
# wikipedia==1.4.0
# google-search-results==2.4.2

# 테스트/UI (Mock 테스트에서는 불필요)
# gradio==4.8.0

# Python 기본 (보통 내장되어 있음)
typing-extensions==4.8.0